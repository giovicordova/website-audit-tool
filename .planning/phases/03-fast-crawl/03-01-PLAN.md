---
phase: 03-fast-crawl
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [SKILL.md]
autonomous: false
requirements: [CRAW-01, CRAW-02, CRAW-03, CRAW-04]

must_haves:
  truths:
    - "Phase B classifies discovered URLs by template type (homepage, blog-post, about, faq, etc.)"
    - "Phase B presents grouped pages to user and waits for selection before crawling"
    - "Phase C crawls only user-selected pages (3-4 typical) instead of 7 most-linked"
    - "Phase C uses navigate_page to reuse existing browser tab (never new_page)"
    - "No snapshots or screenshots taken during crawl phase"
    - "Phase D is removed -- blog posts are just another page in Phase C selection"
    - "Existing scoring and lighthouse regression tests still pass"
  artifacts:
    - path: "SKILL.md"
      provides: "Optimized crawl flow with template diversity, user selection, and browser reuse"
      contains: "template type"
    - path: "SKILL.md"
      provides: "Interactive page selection prompt"
      contains: "Which pages should I audit"
    - path: "SKILL.md"
      provides: "Browser reuse instructions"
      contains: "navigate_page"
  key_links:
    - from: "SKILL.md Phase B"
      to: "SKILL.md Phase C"
      via: "User-selected page list flows from B to C"
      pattern: "selected pages"
    - from: "SKILL.md Phase C"
      to: "modules/extraction.js"
      via: "evaluate_script with extraction function on each page"
      pattern: "extraction"
---

<objective>
Rewrite SKILL.md crawl phases B/C/D to use template-diversity page selection with interactive user choice, browser session reuse via navigate_page, and no-snapshot policy. This reduces crawl from 7 auto-picked pages to 3-4 user-selected pages by template type, bringing total audit time under 1 minute.

Purpose: Audit speed drops from ~2-3 minutes to under 1 minute, and users control which pages get audited.
Output: Updated SKILL.md with optimized crawl flow.
</objective>

<execution_context>
@/Users/giovannicordova/.claude/get-shit-done/workflows/execute-plan.md
@/Users/giovannicordova/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-fast-crawl/03-RESEARCH.md

<!-- Phase 2 established the module-read pattern used in SKILL.md -->
@.planning/phases/02-modularization/02-01-SUMMARY.md

<interfaces>
<!-- Current SKILL.md structure (115 lines after Phase 2 modularization) -->
<!-- The crawl section is lines 25-62. Scoring is lines 87-112. Report is line 113-116. -->
<!-- Phase A (lines 27-38): parallel curl+Playwright+Lighthouse -- stays mostly unchanged -->
<!-- Phase B (lines 39-49): page discovery -- REWRITE for template classification + user selection -->
<!-- Phase C (lines 50-55): sequential crawl -- REWRITE for navigate_page + no snapshots -->
<!-- Phase D (lines 56-62): blog post crawl -- REMOVE (absorbed into Phase C) -->

From SKILL.md current Phase B (to be replaced):
```markdown
#### Phase B: Discover pages to crawl

Combine two sources to build the crawl list:
1. **Sitemap URLs** — all `<loc>` entries from sitemap.xml
2. **Homepage links** — all internal links found by the JS extraction

Pick pages to crawl (in this priority order):
- Up to 5 most-linked internal pages (linked from nav/footer = high priority)
- If a `/blog` or blog listing page is in the list, also crawl **at least 1 individual blog post**
- If the user specified pages (e.g., `/pricing /about`), only visit those instead
```

From SKILL.md current Phase C (to be replaced):
```markdown
#### Phase C: Crawl internal pages (sequential, NOT parallel)

**IMPORTANT:** Crawl pages one at a time using Playwright in the main thread. Do NOT use background agents or parallel tasks for browser navigation — Playwright MCP shares a single browser instance, and concurrent navigations cause stale DOM reads (confirmed in first audit run).

For each page, run a single `browser_evaluate` call with the JS extraction function (see Section 1.1).
```

From SKILL.md current Phase D (to be removed):
```markdown
#### Phase D: Crawl blog post (if applicable)

If a blog listing page was found in Phase B:
1. From the blog listing page's extracted data, pick the first blog post link
2. Navigate to it and run the JS extraction function
3. This verifies Article schema, author visibility, published date, and content depth on actual blog content
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite Phase B with template classification and interactive page selection</name>
  <files>SKILL.md</files>
  <action>
Replace SKILL.md Phase B (lines 39-49) with the new Phase B that has two sub-steps:

**Phase B: Discover and classify pages**

Keep the two-source combination (sitemap + homepage links) but replace the "5 most-linked" logic with template type classification. Use URL path pattern matching:

```
/                           -> "homepage"
/blog, /news, /posts        -> "blog-listing"
/blog/*, /news/*, /posts/*  -> "blog-post"
/about, /team, /company     -> "about"
/faq, /help, /support       -> "faq"
/pricing, /plans            -> "pricing"
/contact, /get-in-touch     -> "contact"
/products/*, /shop/*        -> "product"
/docs/*, /documentation/*   -> "docs"
/case-study/*, /customers/* -> "case-study"
/[single-segment]           -> "landing-page"
```

After classification, add a NEW interactive step:

Present the grouped list to the user:
```
Found {N} pages across {M} template types:

{type} ({count}): {url1}, {url2}, ...

Recommended ({3-4}): {one per unique type, prioritizing types with distinct content patterns}

Which pages should I audit? Pick from above or say "recommended".
```

Wait for user selection before proceeding. Cap "recommended" at 4 pages (1 per unique template type, homepage always included). If user specified pages in the original command (e.g., "audit example.com /pricing /about"), skip the interactive step and crawl those pages directly.

Also update the SKILL.md default description on line 21 from "homepage + up to 5 most-linked internal pages" to "homepage + 3-4 pages selected by template diversity (user chooses)".

Do NOT modify Phase A or any section below Phase D.
  </action>
  <verify>
    <automated>grep -q "template type" SKILL.md && grep -q "Which pages should I audit" SKILL.md && grep -q "Recommended" SKILL.md && echo PASS || echo FAIL</automated>
  </verify>
  <done>Phase B contains template classification instructions with URL pattern list, presents grouped pages to user, waits for selection, and the default description references template diversity instead of most-linked.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Phase C for browser reuse and remove Phase D</name>
  <files>SKILL.md</files>
  <action>
Replace SKILL.md Phase C (lines 50-55) with updated crawl instructions that explicitly use navigate_page and forbid snapshots:

**Phase C: Crawl selected pages (sequential, reuse browser session)**

For each user-selected page:
1. `navigate_page` to URL (reuses existing browser tab -- never use `new_page`)
2. `evaluate_script` with the JS extraction function from modules/extraction.js
3. Do NOT take snapshots or screenshots during crawl -- the extraction function captures all needed data
4. Continue to the next page immediately after extraction completes

Keep the existing warning about sequential-only crawling (single browser instance, concurrent navigations cause stale DOM). But update the tool name references: replace any mention of "browser_evaluate" with "evaluate_script" (the actual chrome-devtools-mcp tool name).

**Remove Phase D entirely.** Blog posts are now just another template type in Phase B's classification. If the user selects a blog post page, it gets crawled in Phase C like any other page. Delete the Phase D section (lines 56-62).

Also check Section 1.1 (JS Extraction Function, line 63-67): if it says "browser_evaluate", change to "evaluate_script". Keep the arrow function warning.

After all edits, run the regression tests to confirm nothing broke:
```bash
bash tests/test-scoring.sh && bash tests/test-lighthouse-output.sh
```

These tests verify scoring logic and lighthouse output parsing. They should pass unchanged since we only modified crawl instructions (natural language), not scoring formulas or test fixtures.
  </action>
  <verify>
    <automated>grep -q "navigate_page" SKILL.md && grep -q "Do NOT take snapshots" SKILL.md && ! grep -q "Phase D" SKILL.md && bash tests/test-scoring.sh && bash tests/test-lighthouse-output.sh && echo PASS || echo FAIL</automated>
  </verify>
  <done>Phase C uses navigate_page with no-snapshot policy, Phase D is removed, tool names updated to evaluate_script, and all 19 regression tests pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify optimized crawl flow with a real audit</name>
  <files>SKILL.md</files>
  <action>
Human verifies the rewritten SKILL.md crawl flow works end-to-end by running a real audit.

What was built:
Rewrote SKILL.md crawl flow -- template-diversity page selection with interactive user choice, browser reuse via navigate_page, no-snapshot policy, and removed Phase D. All regression tests pass.

How to verify:
1. Read SKILL.md and confirm the crawl flow makes sense:
   - Phase A unchanged (parallel curl + homepage + lighthouse)
   - Phase B now classifies pages by template type and asks you to pick
   - Phase C crawls selected pages with navigate_page, no snapshots
   - Phase D no longer exists
2. Run a real audit on any site to verify the new flow works end-to-end and completes in under 1 minute (excluding your selection time):
   "audit [any-site-you-want]"
3. Confirm you see the page selection prompt during the audit
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>Real audit completes in under 1 minute, page selection prompt appears, and user approves the flow.</done>
</task>

</tasks>

<verification>
- `grep -q "template type" SKILL.md` -- template classification present
- `grep -q "Which pages should I audit" SKILL.md` -- interactive selection present
- `grep -q "navigate_page" SKILL.md` -- browser reuse instruction present
- `grep -q "Do NOT take snapshots" SKILL.md` -- no-snapshot policy present
- `! grep -q "Phase D" SKILL.md` -- Phase D removed
- `bash tests/test-scoring.sh` -- all 6 scoring tests pass
- `bash tests/test-lighthouse-output.sh` -- all 13 lighthouse tests pass
</verification>

<success_criteria>
- SKILL.md Phase B classifies pages by template type and presents interactive selection
- SKILL.md Phase C uses navigate_page, evaluate_script, and forbids snapshots
- SKILL.md Phase D is removed (blog posts handled as regular template type)
- All 19 existing regression tests pass
- A real audit (human verification) completes in under 1 minute and shows page selection prompt
</success_criteria>

<output>
After completion, create `.planning/phases/03-fast-crawl/03-01-SUMMARY.md`
</output>
