---
phase: 01-lighthouse-scoring-lock
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/lighthouse.sh
  - references/seo-technical.md
  - SKILL.md
  - tests/test-lighthouse-output.sh
autonomous: true
requirements:
  - PERF-01
  - PERF-02
  - PERF-03

must_haves:
  truths:
    - "Running scripts/lighthouse.sh <url> produces compact JSON with category scores and Core Web Vitals (LCP, CLS, TBT)"
    - "No API key or env var is needed to run lighthouse.sh"
    - "references/seo-technical.md references TBT (not INP) and lighthouse.sh (not pagespeed.sh)"
    - "SKILL.md Phase A step 7 calls lighthouse.sh instead of pagespeed.sh"
    - "test-lighthouse-output.sh validates the JSON shape of lighthouse.sh output"
  artifacts:
    - path: "scripts/lighthouse.sh"
      provides: "Lighthouse CLI wrapper producing compact JSON"
      min_lines: 20
    - path: "references/seo-technical.md"
      provides: "Updated CWV check using TBT and lighthouse.sh"
      contains: "lighthouse.sh"
    - path: "SKILL.md"
      provides: "Updated orchestration referencing lighthouse.sh"
      contains: "lighthouse.sh"
    - path: "tests/test-lighthouse-output.sh"
      provides: "Lighthouse output shape validation test"
      min_lines: 15
  key_links:
    - from: "SKILL.md"
      to: "scripts/lighthouse.sh"
      via: "Phase A step 7 instruction to run lighthouse.sh"
      pattern: "lighthouse\\.sh"
    - from: "references/seo-technical.md"
      to: "scripts/lighthouse.sh"
      via: "CWV check instruction"
      pattern: "lighthouse\\.sh"
    - from: "tests/test-lighthouse-output.sh"
      to: "scripts/lighthouse.sh"
      via: "Runs lighthouse.sh and validates JSON output fields"
      pattern: "lighthouse\\.sh"
---

<objective>
Replace the broken PageSpeed API dependency with a local Lighthouse CLI wrapper and update all references to use it.

Purpose: The current pagespeed.sh requires an API key and hits rate limits. Lighthouse CLI runs locally with zero external dependencies (same engine, better reliability). This also fixes the INP/TBT confusion -- Lighthouse measures TBT, not INP.

Output: Working lighthouse.sh script, updated SKILL.md and reference file, output shape test.
</objective>

<execution_context>
@/Users/giovannicordova/.claude/get-shit-done/workflows/execute-plan.md
@/Users/giovannicordova/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-lighthouse-scoring-lock/01-RESEARCH.md

@scripts/pagespeed.sh
@references/seo-technical.md
@SKILL.md

<interfaces>
<!-- Existing pagespeed.sh output format (being replaced): -->
From scripts/pagespeed.sh:
```python
result = {'lighthouse_scores': scores, 'core_web_vitals': vitals}
# scores = {category: int(score*100)}
# vitals = {metric: {percentile, category}}
```

<!-- New lighthouse.sh output format (from research): -->
Target output shape:
```json
{
  "lighthouse_scores": {
    "performance": 95,
    "accessibility": 87,
    "seo": 80,
    "best_practices": 92
  },
  "core_web_vitals": {
    "lcp": {"value": 800, "display": "0.8 s", "score": 1},
    "cls": {"value": 0.05, "display": "0.05", "score": 1},
    "tbt": {"value": 150, "display": "150 ms", "score": 1}
  }
}
```

<!-- Lighthouse JSON field paths (verified on this system): -->
Category scores: .categories.performance.score (0-1 range, multiply by 100)
CWV metrics: .audits["largest-contentful-paint"], .audits["cumulative-layout-shift"], .audits["total-blocking-time"]
Each audit has: .numericValue, .displayValue, .score
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create lighthouse.sh and update SKILL.md + reference file</name>
  <files>scripts/lighthouse.sh, references/seo-technical.md, SKILL.md</files>
  <action>
1. Create `scripts/lighthouse.sh` (make executable with chmod +x):
   - Shebang: `#!/bin/bash`
   - Usage comment: `# Usage: lighthouse.sh <url>`
   - Input validation: check $1 is provided, exit 1 with usage message if not
   - Run: `npx lighthouse "$URL" --output=json --only-categories=performance,accessibility,seo,best-practices --chrome-flags="--headless" --quiet 2>/dev/null`
   - Error handling: check exit code and empty output, print `{"error": "Lighthouse run failed"}` to stderr and exit 1 on failure
   - Use jq to extract compact JSON with this exact shape:
     ```json
     {
       "lighthouse_scores": {
         "performance": (int, 0-100),
         "accessibility": (int, 0-100),
         "seo": (int, 0-100),
         "best_practices": (int, 0-100)
       },
       "core_web_vitals": {
         "lcp": {"value": (numericValue), "display": (displayValue), "score": (score)},
         "cls": {"value": (numericValue), "display": (displayValue), "score": (score)},
         "tbt": {"value": (numericValue), "display": (displayValue), "score": (score)}
       }
     }
     ```
   - jq expression from research: multiply category scores by 100 and floor, extract audit fields for LCP/CLS/TBT
   - Follow existing convention from pagespeed.sh: quote variables, use stderr for errors

2. Update `references/seo-technical.md`:
   - CRITICAL section, CWV check (line 16): change from `Core Web Vitals pass (run pagespeed.sh): LCP < 2.5s, INP < 200ms, CLS < 0.1` to `Core Web Vitals pass (run lighthouse.sh): LCP < 2.5s, TBT < 200ms (lab proxy for INP), CLS < 0.1`
   - Same line: change conditional from "if PageSpeed API fails" to "if Lighthouse fails"
   - IMPORTANT section (line 23): change "Lighthouse performance score >= 50" conditional from "if PageSpeed API fails" to "if Lighthouse fails"
   - NICE TO HAVE section (lines 28-29): same conditional update for performance >= 90 and accessibility >= 90

3. Update `SKILL.md`:
   - Find the Phase A step that references pagespeed.sh (around line ~40-50 area where phase steps are listed)
   - Replace reference to `pagespeed.sh` with `lighthouse.sh`
   - Update the description: "returns JSON with performance/accessibility/seo/best-practices scores and Core Web Vitals (LCP, CLS, TBT). No API key needed. If it fails, mark CWV checks as UNTESTABLE and continue."
   - Remove any reference to PAGESPEED_API_KEY requirement

Do NOT delete pagespeed.sh yet -- it stays until migration is confirmed working.
  </action>
  <verify>
    <automated>bash -n scripts/lighthouse.sh && test -x scripts/lighthouse.sh && grep -q "lighthouse.sh" references/seo-technical.md && grep -q "TBT" references/seo-technical.md && grep -q "lighthouse.sh" SKILL.md && echo "PASS"</automated>
  </verify>
  <done>lighthouse.sh is executable, uses npx lighthouse with --headless, outputs compact JSON via jq. references/seo-technical.md uses TBT (not INP) and references lighthouse.sh (not pagespeed.sh). SKILL.md calls lighthouse.sh.</done>
</task>

<task type="auto">
  <name>Task 2: Create lighthouse output shape test</name>
  <files>tests/test-lighthouse-output.sh</files>
  <action>
Create `tests/test-lighthouse-output.sh` (make executable):

This test validates that lighthouse.sh produces correctly shaped JSON WITHOUT running a live Lighthouse audit. It does this by:

1. Creating a mock Lighthouse JSON file (in /tmp) that mimics the raw Lighthouse output structure with the fields that jq extracts:
   ```json
   {
     "categories": {
       "performance": {"score": 0.95},
       "accessibility": {"score": 0.87},
       "seo": {"score": 0.80},
       "best-practices": {"score": 0.92}
     },
     "audits": {
       "largest-contentful-paint": {"numericValue": 1200, "displayValue": "1.2 s", "score": 0.9},
       "cumulative-layout-shift": {"numericValue": 0.05, "displayValue": "0.05", "score": 1},
       "total-blocking-time": {"numericValue": 150, "displayValue": "150 ms", "score": 1}
     }
   }
   ```

2. Running the jq extraction command from lighthouse.sh against this mock JSON (extract the jq expression from lighthouse.sh and apply it to the mock file -- this tests the jq filter in isolation)

3. Asserting the output has the expected fields and values:
   - `.lighthouse_scores.performance` == 95
   - `.lighthouse_scores.accessibility` == 87
   - `.lighthouse_scores.seo` == 80
   - `.lighthouse_scores.best_practices` == 92
   - `.core_web_vitals.lcp.value` == 1200
   - `.core_web_vitals.cls.value` == 0.05
   - `.core_web_vitals.tbt.value` == 150

Use an assert_eq function (same pattern as test-scoring.sh from research):
```bash
assert_eq() {
  local label="$1" expected="$2" actual="$3"
  if [[ "$expected" == "$actual" ]]; then
    echo "PASS: $label"; ((PASS++))
  else
    echo "FAIL: $label (expected=$expected, actual=$actual)"; ((FAIL++))
  fi
}
```

Clean up temp files at exit. Print summary. Exit 1 if any failures.

Also verify lighthouse.sh exists and is executable as a precondition (fail fast with clear message if not).
  </action>
  <verify>
    <automated>bash tests/test-lighthouse-output.sh</automated>
  </verify>
  <done>test-lighthouse-output.sh runs in under 2 seconds, validates jq extraction against mock Lighthouse JSON, all assertions pass. Covers PERF-01 (correct fields), PERF-02 (no API key in script), PERF-03 (structured output with LCP, CLS, TBT, category scores).</done>
</task>

</tasks>

<verification>
- `bash -n scripts/lighthouse.sh` -- syntax check passes
- `bash tests/test-lighthouse-output.sh` -- all shape assertions pass
- `grep -c "pagespeed.sh" references/seo-technical.md` returns 0 (no remaining pagespeed references)
- `grep -c "INP" references/seo-technical.md` returns 0 (TBT used instead)
- `grep "lighthouse.sh" SKILL.md` shows the updated reference
</verification>

<success_criteria>
- lighthouse.sh is executable and produces compact JSON from Lighthouse CLI output
- No API key needed anywhere in the Lighthouse flow
- references/seo-technical.md uses TBT and lighthouse.sh throughout
- SKILL.md references lighthouse.sh instead of pagespeed.sh
- test-lighthouse-output.sh validates the JSON extraction and passes
</success_criteria>

<output>
After completion, create `.planning/phases/01-lighthouse-scoring-lock/01-01-SUMMARY.md`
</output>
