---
phase: 02-modularization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - modules/extraction.js
  - modules/report-template.md
  - SKILL.md
autonomous: true
requirements: [MODU-01, MODU-02, MODU-03, MODU-04]

must_haves:
  truths:
    - "JS extraction function lives in modules/extraction.js and is referenced by SKILL.md"
    - "Report template lives in modules/report-template.md and is referenced by SKILL.md"
    - "Scoring formula remains inline in SKILL.md"
    - "SKILL.md is under 150 lines and contains only orchestration + scoring logic"
    - "All Phase 1 scoring and lighthouse tests still pass"
  artifacts:
    - path: "modules/extraction.js"
      provides: "JS extraction function for browser_evaluate"
      contains: "() => {"
    - path: "modules/report-template.md"
      provides: "Report template with all placeholders and formatting"
      contains: "Website Audit: {domain}"
    - path: "SKILL.md"
      provides: "Orchestration-only skill file under 150 lines"
      contains: "Critical checks: 3 points"
  key_links:
    - from: "SKILL.md"
      to: "modules/extraction.js"
      via: "Read instruction in Section 1.1"
      pattern: "Read.*modules/extraction\\.js"
    - from: "SKILL.md"
      to: "modules/report-template.md"
      via: "Read instruction in Section 5"
      pattern: "Read.*modules/report-template\\.md"
---

<objective>
Split SKILL.md from a 306-line monolith into a short orchestrator that dispatches to focused module files.

Purpose: Make edits safe and isolated -- changing the JS extraction function or report template no longer risks breaking orchestration logic, and vice versa. Phase 1 scoring tests serve as the safety net.
Output: modules/extraction.js, modules/report-template.md, updated SKILL.md (~133-146 lines)
</objective>

<execution_context>
@/Users/giovannicordova/.claude/get-shit-done/workflows/execute-plan.md
@/Users/giovannicordova/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-modularization/02-RESEARCH.md

<interfaces>
<!-- Current SKILL.md structure (306 lines) -->
<!-- Lines 63-176: JS extraction function (arrow function for browser_evaluate) -->
<!-- Lines 224-284: Report template (markdown with {placeholders}) -->
<!-- Lines 199-222: Scoring formula (STAYS INLINE per MODU-03) -->
<!-- Lines 286-307: Compare mode (stays inline, ~21 lines) -->

<!-- Existing file reference pattern already used in SKILL.md: -->
From SKILL.md line 182:
```markdown
Read the reference files for the requested categories from this skill's `references/` directory
```
<!-- New module references must follow this same phrasing pattern -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract JS function and report template into module files</name>
  <files>modules/extraction.js, modules/report-template.md</files>
  <action>
Create `modules/` directory.

**modules/extraction.js:**
Copy SKILL.md lines 68-175 verbatim (the arrow function body from `() => {` through the closing `}`). Add a comment header:
```
// JS Extraction Function for Website Audit Skill
// Used by SKILL.md via browser_evaluate on every crawled page.
// IMPORTANT: This MUST be an arrow function, NOT an IIFE.
// Playwright MCP rejects self-invoking functions.
```
Then the full function. Do NOT wrap in IIFE. Do NOT add module.exports. This is a raw arrow function body that Claude copy-pastes into browser_evaluate.

**modules/report-template.md:**
Copy SKILL.md lines 224-284 verbatim -- everything from `### 5. Report` through the end of the fix priority list instructions (before `## Compare Mode`). This includes:
- The conversational summary instruction ("First: conversational summary in chat...")
- The file path pattern (`docs/w-audit/audit-{domain}-{YYYY-MM-DD}T{HH-MM}.md`)
- The mkdir instruction
- The full markdown template with ALL placeholders
- The fix priority list ordering rules (red/yellow/green circles)
- The audit log save instruction

Also include the Compare Mode section (lines 286-307) at the end of this file, since it's report formatting. This keeps ALL output formatting in one place (per research recommendation in Open Questions).
  </action>
  <verify>
    <automated>test -f modules/extraction.js && test -f modules/report-template.md && echo PASS || echo FAIL</automated>
  </verify>
  <done>Both module files exist with verbatim content from SKILL.md. extraction.js contains the arrow function with comment header. report-template.md contains the full report template, audit log instruction, and compare mode section.</done>
</task>

<task type="auto">
  <name>Task 2: Update SKILL.md to reference modules and verify regression tests</name>
  <files>SKILL.md</files>
  <action>
Rewrite SKILL.md replacing the extracted blocks with file-read references. Keep everything else intact.

**Replace Section 1.1 (lines 63-178)** with:
```markdown
### 1.1 JS Extraction Function

Read `modules/extraction.js` from this skill's directory. Run this exact function (or a superset of it) via a single `browser_evaluate` call on every page. Do not extract metadata piecemeal across multiple calls.

**IMPORTANT:** The function must be an arrow function, NOT an IIFE -- Playwright MCP rejects self-invoking functions.
```

**Replace Section 5 and Compare Mode (lines 224-307)** with:
```markdown
### 5. Report and Compare Mode

Read `modules/report-template.md` from this skill's directory. Follow the exact format for both single-site audits and compare mode. Do not improvise sections or reorder categories.
```

**Keep inline (do NOT extract):**
- Lines 1-9: YAML frontmatter
- Lines 10-62: Request parsing + Crawl phases A-D (orchestration)
- Lines 180-222: Load Rules + Check Each Category + Scoring formula (per MODU-03)

After rewriting, verify:
1. `wc -l < SKILL.md` is 150 or less
2. `grep -q "Critical checks: 3 points" SKILL.md` confirms scoring formula is still inline
3. `bash tests/test-scoring.sh` passes (scoring regression test)
4. `bash tests/test-lighthouse-output.sh` passes (lighthouse regression test)

If line count exceeds 150, trim unnecessary blank lines or tighten wording in orchestration sections (NOT in the scoring formula).
  </action>
  <verify>
    <automated>[ $(wc -l < SKILL.md) -le 150 ] && grep -q "Critical checks: 3 points" SKILL.md && bash tests/test-scoring.sh && bash tests/test-lighthouse-output.sh && echo PASS || echo FAIL</automated>
  </verify>
  <done>SKILL.md is under 150 lines. Scoring formula is inline. Both module files are referenced with "Read from this skill's directory" pattern. All Phase 1 regression tests pass.</done>
</task>

</tasks>

<verification>
1. `test -f modules/extraction.js` -- JS extraction file exists (MODU-01)
2. `test -f modules/report-template.md` -- Report template file exists (MODU-02)
3. `grep -q "Critical checks: 3 points" SKILL.md` -- Scoring formula stays inline (MODU-03)
4. `[ $(wc -l < SKILL.md) -le 150 ]` -- SKILL.md under 150 lines (MODU-04)
5. `bash tests/test-scoring.sh` -- Scoring regression tests pass
6. `bash tests/test-lighthouse-output.sh` -- Lighthouse regression tests pass
</verification>

<success_criteria>
- modules/extraction.js contains the complete JS extraction arrow function
- modules/report-template.md contains the complete report template + compare mode format
- SKILL.md references both modules using the established "Read from this skill's directory" pattern
- SKILL.md is 150 lines or fewer
- Scoring formula remains inline in SKILL.md
- All Phase 1 tests pass unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/02-modularization/02-01-SUMMARY.md`
</output>
